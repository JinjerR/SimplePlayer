<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@id/tracks"
        android:title="@string/tracks"
        app:showAsAction="ifRoom"
        android:orderInCategory="1"
        android:icon="@drawable/icon_navigation_tracks"/>

    <item android:id="@id/folders"
        android:title="@string/folders"
        app:showAsAction="ifRoom"
        android:orderInCategory="2"
        android:icon="@drawable/icon_navigation_folders"/>

    <item android:id="@id/search"
        android:title="@string/search"
        app:showAsAction="ifRoom"
        android:orderInCategory="3"
        android:icon="@drawable/icon_navigation_search"/>
</menu>